spring:
  datasource:
    url: jdbc:h2:/Users/dkurnikov/IdeaProjects/demo/demoDataSource
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  batch:
    job:
      enabled: false
management:
  endpoint:
    health:
      show-details: always
spark:
  fileName: data/sparkDemo
  appName: SparkDemoSpring
  master: local[2]

